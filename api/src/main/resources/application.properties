spring.application.name=loginsight-api
server.port=8081

# Java 21 virtual threads replace the Tomcat thread pool — no WebFlux needed
spring.threads.virtual.enabled=true

# Jackson: serialize Instant as ISO-8601 string, not epoch array
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false

# Actuator — health only (metrics are exported by OTel PrometheusHttpServer on port 9464)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# Don't leak stack traces / framework internals in error responses
server.error.include-stacktrace=never
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-exception=false

# Logging
logging.level.com.loginsight=INFO
logging.level.org.apache.kafka=WARN
logging.level.co.elastic=WARN
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# Storage backends (empty = disabled, warns at startup)
loginsight.elasticsearch.url=${ELASTICSEARCH_URL:}
loginsight.influxdb.url=${INFLUXDB_URL:}
loginsight.influxdb.token=${INFLUXDB_TOKEN:}
loginsight.influxdb.org=${INFLUXDB_ORG:}
loginsight.influxdb.bucket=${INFLUXDB_BUCKET:}

# Kafka wiring (alert subscriber + log simulator)
loginsight.kafka.bootstrap-servers=${LOGINSIGHT_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
loginsight.kafka.alerts-topic=${LOGINSIGHT_KAFKA_ALERTS_TOPIC:anomaly-alerts}
loginsight.kafka.raw-topic=${LOGINSIGHT_KAFKA_RAW_TOPIC:raw-logs}
loginsight.kafka.enabled=${LOGINSIGHT_KAFKA_ENABLED:true}

# Simulation control protection — set a strong secret to require X-Simulation-Key header
# Leave blank to allow unauthenticated access (acceptable for localhost-only deployments)
loginsight.simulation.api-key=${SIMULATION_API_KEY:}

# CORS — comma-separated origins allowed to call /api/** cross-origin
# Leave blank to deny all cross-origin requests (recommended for production)
loginsight.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:}

# JWT — replace jwt.secret with: openssl rand -base64 32
jwt.secret=${JWT_SECRET:bG9naW5zaWdodC1zZWNyZXQta2V5LW11c3QtYmUtYXQtbGVhc3QtMzItYnl0ZXMh}
jwt.expiration-ms=86400000
