# Copy this to .env.ps1 (gitignored) and fill in values.
# Dot-source before running run-api.ps1 / run-ingestion.ps1:
#   . .\.env.ps1

# Must match INFLUXDB_ADMIN_TOKEN in your .env file
$env:INFLUXDB_TOKEN = 'change-me-same-as-env-file'
